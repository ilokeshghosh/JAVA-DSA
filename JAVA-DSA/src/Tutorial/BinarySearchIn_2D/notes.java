package Tutorial.BinarySearchIn_2D;

public class notes {
    /*
   # Search In sorted 2-D matrix
   cases:
   1) if element == target
            // ans
   2) if element > target
            //Ignore rows after it

   3) if element<target
            // Ignore rows above it

   In the end 2 rows are remaining:

   1) Check whether the  mid col you are at contains the ans
   2)Consider the four parts

    */
}
